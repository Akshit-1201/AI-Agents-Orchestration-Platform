[pytest]
asyncio_mode = auto
testpaths = tests
# Add backend/ (the rootdir, where this file lives) to sys.path so tests can
# `from main import app`, `from models import ...`, etc. regardless of cwd.
pythonpath = .
